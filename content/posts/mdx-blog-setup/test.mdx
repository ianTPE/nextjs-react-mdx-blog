# Test MDX File

This is a test file to check if syntax highlighting is working.

```javascript
// Here's some JavaScript code
function testFunction() {
  const greeting = "Hello, world!";
  console.log(greeting);
  return true;
}

// Let's test some conditionals
if (true) {
  console.log("This is true!");
} else {
  console.log("This should never run");
}
```

```html
<!-- Here's some HTML code -->
<div class="container">
  <h1>Hello World</h1>
  <p>This is a paragraph of HTML code</p>
  <button onclick="alert('clicked!')">Click me</button>
</div>
```

```css
/* Some CSS code */
.container {
  background-color: #f5f5f5;
  padding: 20px;
  border-radius: 8px;
}

h1 {
  color: #333;
  font-size: 24px;
}
```
